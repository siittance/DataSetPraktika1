<Window x:Class="DataSetPrakt1.Pets"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DataSetPrakt1"
        mc:Ignorable="d"
        Title="Pets" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="90"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Border BorderBrush="Black" BorderThickness="2">
            <TextBlock x:Name="tekstik" Text="Таблица питомцев" Background="AntiqueWhite" FontSize="22" TextAlignment="Center"/>
        </Border>
        <DataGrid x:Name="GridDataPets" Grid.Row="1" Grid.ColumnSpan="2" Background="LightYellow"/>
        <Button x:Name="exitt" Grid.Column="1" Content="Выход" Background="BlanchedAlmond" Click="clickkk"/>
    </Grid>
</Window>
